[package]
name = "mpc"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Cryptographic payment authorization project - multi-party computation core"

[dependencies]
ark-ff = "0.4"
ark-std = "0.4"
crypto-core = { path = "../crypto-core", version = "0.1.0" }
rand_core = "0.6"
secret-sharing = { path = "../secret-sharing", version = "0.1.0" }

[dev-dependencies]
ark-ed25519 = "0.4"
rand_chacha = "0.3"
