[package]
name = "sdk"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Cryptographic payment authorization project - client SDK"

[dependencies]
payment = { path = "../payment", version = "0.1.0" }
proof = { path = "../proof", version = "0.1.0" }
policy = { path = "../policy", version = "0.1.0" }
verifier = { path = "../verifier", version = "0.1.0" }
