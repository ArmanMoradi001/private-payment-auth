[package]
name = "sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
payment = { path = "../payment" }
proof = { path = "../proof" }
policy = { path = "../policy" }
verifier = { path = "../verifier" }
