//! Public SDK for integrating cryptographic payment authorization.
//!
//! This crate will be the single entry point for external consumers,
//! re-exporting a stable API over `payment`, `proof`, `policy`, and
//! `verifier` functionality.
#![forbid(unsafe_code)]
#![warn(missing_docs)]
