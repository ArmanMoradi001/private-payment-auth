[package]
name = "mpcith"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Cryptographic payment authorization project - MPC-in-the-head proof system"

[dependencies]
ark-ed25519 = "0.4"
ark-ff = "0.4"
circuit = { path = "../circuit", version = "0.1.0" }
crypto-core = { path = "../crypto-core", version = "0.1.0" }
mpc = { path = "../mpc", version = "0.1.0" }
rand_core = "0.6"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
proptest = "1.4"
rand_chacha = "0.3"

[[bench]]
name = "mpcith_bench"
harness = false
